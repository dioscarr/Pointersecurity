@model IEnumerable<PointerSecurityDataLayer.Module>
@{
    if (@Model.Count() > 0)
    {
        foreach (var item in Model)
        {
            <div class="col-md-2 text-center servicescells hollow3"
                 style="">
                    @item.ServiceName
            </div>
        }
    }
}