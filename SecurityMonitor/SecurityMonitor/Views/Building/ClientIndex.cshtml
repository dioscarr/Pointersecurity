@model IEnumerable<SecurityMonitor.Models.ClientsVM>
@using SecurityMonitor.Models;
@{
    ViewBag.Title = "ClientIndex";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<h2>ClientIndex</h2>

<p>
    @Html.ActionLink("Add a new client", "AddClient")
</p>*@
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClientName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BuildingCount)
        </th>
        <th></th>
    </tr>*@

@*@foreach (var item in Model) {
    <tr>
        <td>
            <button type="button" 
                    class="btn btn-link" 
                    onclick="location.href = '@Url.Action("BuildingIndex", "Building", new {ClientID = item.ID })'">
                    @Html.DisplayFor(modelItem => item.ClientName)
            </button>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BuildingCount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}*@

@*</table>*@

<!--main layout for this page-->
<div class="row">
    <!--visible on sm xs-->
    <div class="hidden-md hidden-lg visible-sm visible-xs col-sm-12 col-xs-12  tile_height lightblue "></div>

    <!--visible on md lg-->
    <div class="hidden-xs hidden-sm visible-md visible-lg col-md-2 col-lg-2  tile_height lightblue text-center">
        @using(Html.BeginForm()){
            <br/>
            <h5>Client's Name</h5>
             @Html.TextBox("ClientName", null, new {@class="form-control" })
            <hr/>
            <h5>Client's Address</h5>
             @Html.TextBox("ClientAddress", null, new { @class = "form-control" })
             <hr/>
            <h5>City</h5>
             @Html.TextBox("ClientCity", null, new { @class = "form-control" })
             <hr/>
            <h5>State</h5>
             @Html.TextBox("ClientState", null, new { @class = "form-control" })
             <hr/>
            <h5>Zipcode</h5>
             @Html.TextBox("ClientZipcode", null, new { @class = "form-control" })
             <hr/>
            <h5>Phone</h5>
             @Html.TextBox("ClientPhone", null, new { @class = "form-control" })
             <hr/>
            
        <input class="btn form-control" type="submit" value="Submit"/>
        }
    </div>

    @foreach(var item in Model){
   
    <!--always visible main content -->
  
      <!--title or information-->
        <div class="col-lg-6 col-md-6 col-sm-8 col-xs-8 text-center  hollow lightblue " style="padding-bottom:8%;">
            <h4 style="position:absolute; top:30%; left:10%; ">@item.ClientName</h4>
            
        </div>
       <!--edit-->
       <div class="col-md-2 col-sm-2 col-xs-2 text-center  hollow lightdarkblue " style="padding-bottom:8%;">
           <img src="~/img/edit.png" style="position:absolute; left:42%; top:10%; height:25%; width:15%;" />
           <h4 style="position:absolute; left:40%; top:38%;">Edit</h4>
       </div>
       <!--trash-->
       <div class="col-md-2 col-sm-2 col-xs-2 text-center pull-right hollow darkblue " style=" padding-bottom:8%;">
           <img src="~/img/trash.png" style=" position:absolute; left:42%; top:10%; height:25%; width:15%;" />
           <h4 style="position:absolute; left:40%; top:38%;">Trash</h4>
       </div>

   
         }

</div>
